<Window x:Class="_1Demo.Staff"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_1Demo"
        mc:Ignorable="d"
        Title="Товары" Height="450" Width="800" FontFamily="Times New Roman">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Button x:Name="Back" HorizontalAlignment="Left" VerticalAlignment="Center" Content="Назад" Height="20" Width="50" Grid.Column="1" Click="Back_Click"/>
        <TextBlock x:Name="loginName" Grid.Row="0" Grid.Column="0" HorizontalAlignment="Right"/>
        <ScrollViewer Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2">
            <StackPanel x:Name="stackPanel" Background="Black"></StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
